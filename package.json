{
  "name": "booktales-mern",
  "version": "1.0.0",
  "description": "BookTales - A book recommendation website built with MERN stack",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd client && npm run build",
    "start": "cd backend && npm start",
    "seed": "cd backend && npm run seed"
  },
  "keywords": [
    "mern",
    "react",
    "mongodb",
    "express",
    "book-recommendation"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "csv": "^6.5.1",
    "express-validator": "^7.3.2"
  }
}
