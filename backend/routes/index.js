const auth = require('./auth');
const books = require('./books');
const reviews = require('./reviews');
const admin = require('./admin');
const dashboard = require('./dashboard');

module.exports = { auth, books, reviews, admin, dashboard };
